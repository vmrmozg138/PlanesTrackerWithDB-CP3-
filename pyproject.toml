[tool.poetry]
name = "planestracker(courseproject2)"
version = "0.1.0"
description = ""
authors = ["Vladimir Rastorguev <vmrmozg138@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.14"
requests = "^2.34.2"
pandas = "^3.0.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
